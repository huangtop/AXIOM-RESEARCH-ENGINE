# V030.11.2 — Source Router

Adds a SEC-first, field-level financial source router. SEC timeline values remain primary; Yahoo company snapshots fill only missing metrics. Every selected metric records provider, confidence, source state, period/source field, and fallback reason. Missing values remain diagnostic rather than company-level failures.
